pro[] build(): non = {
    var build = .build();
    build.meta({ name = "std", version = "0.1.0" });
    var graph = build.graph();
    var lib = graph.add_static_lib({
        name = "std",
        root = "lib.fol",
        fol_model = "std",
    });
    graph.install(lib);
};
