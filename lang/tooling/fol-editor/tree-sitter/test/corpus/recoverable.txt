==================
Recoverable
==================

use shared: loc = {shared::lib}

fun load(flag: bol): int / str = {
    when(flag) {
        report "bad-input"
    }
    return 7
}

fun demo(flag: bol): int = {
    return load(flag) || 0
}
