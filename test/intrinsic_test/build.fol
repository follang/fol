pro[] build(): non = {
    var graph = .graph();
    var app = graph.add_exe({
        name = "intrinsic_test",
        root = "src/main.fol",
    });

    graph.install(app);
    graph.add_run(app);
};
