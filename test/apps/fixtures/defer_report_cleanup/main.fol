fun[] show(value: int): int = {
    return .echo(value);
};

fun[] main(): int / str = {
    defer {
        show(1);
    };
    when(true) {
        case(true) { report "main-bad"; }
        * { return show(7); }
    };
};
