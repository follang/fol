typ Counter: rec = {
    total: int
};

fun (Counter)shift(by: int, step: int = 2): int = {
    return by + step;
};

fun[] main(): int = {
    var current: Counter = {
        total = 9,
    };
    return current.shift(step = 3);
};
