typ Counter: rec = {
    total: int
};

fun (Counter)shift(by: int, step: int): int = {
    return by + step;
};

fun[] main(): int = {
    var current: Counter = {
        total = 9,
    };
    return current.shift(missing = 3, by = 1);
};
