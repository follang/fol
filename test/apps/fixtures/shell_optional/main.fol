ali MaybeScore: opt[int];

fun[] pick(flag: bol): MaybeScore = {
    when(flag) {
        case(true) { return 7; }
        * { return nil; }
    }
}

fun[] main(flag: bol): int = {
    when(flag) {
        case(true) { return pick(true)!; }
        * { return 0; }
    }
}
