pro[] build(): non = {
    var graph = .graph();
    var target = graph.standard_target();
    var optimize = graph.standard_optimize();
    var lib = graph.add_static_lib({
        name = "shared",
        root = "src/lib.fol",
        target = target,
        optimize = optimize,
    });

    graph.install(lib);
};
