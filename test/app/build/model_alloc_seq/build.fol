pro[] build(): non = {
    var graph = .graph();
    var app = graph.add_exe({
        name = "model_alloc_seq",
        root = "src/main.fol",
        fol_model = "alloc",
    });

    graph.install(app);
};
