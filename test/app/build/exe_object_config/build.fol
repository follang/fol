pro[] build(): non = {
    var build = .build();
    build.meta({ name = "exe_object_config", version = "0.1.0" });
    var graph = build.graph();
    var target = graph.standard_target();
    var optimize = graph.standard_optimize();
    var app = graph.add_exe({
        name = "exe_object_config",
        root = "src/main.fol",
        fol_model = "std",
        target = target,
        optimize = optimize,
    });

    graph.install(app);
    graph.add_run(app);
};
