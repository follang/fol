pro[] build(): non = {
    var graph = .graph();
    var app = graph.add_exe({
        name = "model_core_foundation",
        root = "src/main.fol",
        fol_model = "core",
    });

    graph.install(app);
};
