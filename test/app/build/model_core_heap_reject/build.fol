pro[] build(graph: Graph): non = {
    var app = graph.add_exe({
        name = "model_core_heap_reject",
        root = "src/main.fol",
        fol_model = "core",
    });

    graph.install(app);
};
