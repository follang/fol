pro[] build(): non = {
    var build = .build();
    build.meta({ name = "static_lib", version = "0.1.0" });
    var graph = build.graph();
    var lib = graph.add_static_lib({
        name = "static_lib",
        root = "src/lib.fol",
    });

    graph.install(lib);
};
