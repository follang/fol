pro[] build(): non = {
    var build = .build();
    build.meta({ name = "exe_basic", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "exe_basic",
        root = "src/main.fol",
    });

    graph.install(app);
    graph.add_run(app);
};
