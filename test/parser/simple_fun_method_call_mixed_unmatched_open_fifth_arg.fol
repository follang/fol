fun method_mixed_unmatched_open_fifth_arg(obj: int, a: int, b: int, c: int, d: int, e: int): int = {
    obj.run(
        a,
        b,
        c,
        d,
        inner(
            (e + f,
            g,
        ),
    );

    return e;
}
