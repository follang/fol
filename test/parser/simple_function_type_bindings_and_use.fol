use callback: {fun loader(path: str): int} = {pkg::loader};

var transform: {fun (value: int): int};

fun local_binding(): int = {
    let formatter: {fun (source: str): str} = callback;
    return 0;
};
