fun call_with_comments(a: int, b: int): int = {
    value = combine(
        a,
        `keep b wrapped for tracing`
        wrap(
            b,
        ),
        `include stable
        literal for checksum`
        42,
    );

    return emit(
        value,
    );
}
