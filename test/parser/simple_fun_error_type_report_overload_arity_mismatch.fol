fun make_err(code: int): int = {
    return code;
}

fun make_err(code: int, mode: int): str = {
    return "err";
}

fun caller_overload_bad(a: int): int : str = {
    report make_err(a);
    return 0;
}
