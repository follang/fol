pro caller_forward_overload_ok(a: int, b: int): int : str = {
    report make_err(a, b);
    return 0;
}

pro make_err(code: int): int = {
    return code;
}

pro make_err(code: int, mode: int): str = {
    return "err";
}
