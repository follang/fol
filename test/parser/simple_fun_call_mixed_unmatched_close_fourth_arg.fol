fun mixed_unmatched_close_fourth_arg(a: int, b: int, c: int, d: int): int = {
    value = outer(
        a,
        b,
        c,
        inner(
            d + ),
            f,
        ),
    );

    return value;
}
