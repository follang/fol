fun mixed_unmatched_open_fourth_arg(a: int, b: int, c: int, d: int): int = {
    value = outer(
        a,
        b,
        c,
        inner(
            (d + e,
            f,
        ),
    );

    return value;
}
