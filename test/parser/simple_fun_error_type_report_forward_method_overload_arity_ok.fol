fun caller_forward_method_overload_ok(tool: parser, a: int, b: int): int / str = {
    report tool.make_err(a, b);
    return 0;
}

fun (parser)make_err(code: int): int = {
    return code;
}

fun (parser)make_err(code: int, mode: int): str = {
    return "err";
}
