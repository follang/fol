fun mixed_unmatched_close_fifth_arg(a: int, b: int, c: int, d: int, e: int): int = {
    value = outer(
        a,
        b,
        c,
        d,
        inner(
            e + ),
            g,
        ),
    );

    return value;
}
