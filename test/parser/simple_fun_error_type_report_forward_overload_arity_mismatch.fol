fun caller_forward_overload_bad(a: int): int / str = {
    report make_err(a);
    return 0;
};

fun make_err(code: int): int = {
    return code;
};

fun make_err(code: int, mode: int): str = {
    return "err";
};
