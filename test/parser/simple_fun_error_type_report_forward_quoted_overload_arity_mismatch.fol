fun caller_forward_quoted_overload_mismatch(a: int, b: int): int / str = {
    report "$make_err"(a, b);
    return 0;
};

fun "$make_err"(code: int): str = {
    return "ok";
};

fun "$make_err"(code: int, mode: int): int = {
    return code + mode;
};
