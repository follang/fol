typ Parser: rec = { };

fun caller_forward_single_quoted_method_mismatch(tool: Parser): int / str = {
    report tool.'$make_err'();
    return 0;
};

fun (Parser)'$make_err'(): int = {
    return 1;
};
