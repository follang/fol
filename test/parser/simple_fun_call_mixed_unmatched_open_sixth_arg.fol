fun mixed_unmatched_open_sixth_arg(a: int, b: int, c: int, d: int, e: int, f: int): int = {
    value = outer(
        a,
        b,
        c,
        d,
        e,
        inner(
            (f + g,
            h,
        ),
    );

    return value;
}
