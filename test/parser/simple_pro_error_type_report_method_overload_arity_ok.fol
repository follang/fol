pro (parser)make_err(code: int): int = {
    return code;
}

pro (parser)make_err(code: int, mode: int): str = {
    return "err";
}

pro caller_method_overload_ok(tool: parser, a: int, b: int): int : str = {
    report tool.make_err(a, b);
    return 0;
}
