pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_memo_echo", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "fail-memo-echo",
        root = "src/main.fol",
        fol_model = "memo",
    });
    graph.install(app);
    return;
};
