pro[] build(graph: Graph): non = {
    var app = graph.add_exe({
        name = "alloc-defaults",
        root = "src/main.fol",
        fol_model = "alloc",
    });
    graph.install(app);
    return graph;
};
