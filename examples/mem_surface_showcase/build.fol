pro[] build(): non = {
    var build = .build();
    build.meta({ name = "mem_surface_showcase", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "mem-surface-showcase",
        root = "src/main.fol",
        fol_model = "mem",
    });
    graph.install(app);
    return;
};
