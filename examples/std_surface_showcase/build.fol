pro[] build(): non = {
    var build = .build();
    build.meta({ name = "std_surface_showcase", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "std-surface-showcase",
        root = "src/main.fol",
        fol_model = "std",
    });
    graph.install(app);
    graph.add_run(app);
    return;
};
