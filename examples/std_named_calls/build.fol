pro[] build(graph: Graph): non = {
    var app = graph.add_exe({
        name = "std-named-calls",
        root = "src/main.fol",
        fol_model = "std",
    });
    graph.install(app);
    graph.add_run(app);
    return graph;
};
