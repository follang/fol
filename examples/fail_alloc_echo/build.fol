pro[] build(): non = {
    var build = .build();
    build.meta({ name = "fail_alloc_echo", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "fail-alloc-echo",
        root = "src/main.fol",
        fol_model = "mem",
    });
    graph.install(app);
    return;
};
