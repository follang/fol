typ Counter: rec = {
    total: int,
    step: int,
};

fun (Counter)read(by: int = 1): int = {
    return by + 2;
};

fun[] main(): int = {
    var counter: Counter = {
        total = 3,
        step = 2,
    };
    return counter.read();
};
