fun[] checksum(values: arr[int, 3], seed: int): int = {
    return seed + .len(values) + values[0] + values[1] + values[2];
};

fun[] main(): int = {
    var pins: arr[int, 3] = {1, 0, 1};
    return checksum(pins, 4);
};
