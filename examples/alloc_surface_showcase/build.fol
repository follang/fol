pro[] build(): non = {
    var build = .build();
    build.meta({ name = "alloc_surface_showcase", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "alloc-surface-showcase",
        root = "src/main.fol",
        fol_model = "alloc",
    });
    graph.install(app);
    return;
};
