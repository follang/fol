fun[] banner(prefix: str = "std", extras: ... str): str = {
    return prefix + extras[0];
};

fun[] main(): int = {
    var parts: seq[str] = {"-tool"};
    var text: str = banner(...parts);
    var shown: str = .echo(text);
    return 7;
};
