pro[] build(): non = {
    var build = .build();
    build.meta({ name = "std_substrate_echo", version = "0.1.0" });
    build.add_dep({ alias = "std", source = "internal", target = "standard" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "std-substrate-echo",
        root = "src/main.fol",
    });
    graph.install(app);
    graph.add_run(app);
    return;
};
