pro[] build(): non = {
    var build = .build();
    build.meta({ name = "memo_defaults", version = "0.1.0" });
    var graph = build.graph();
    var app = graph.add_exe({
        name = "memo-defaults",
        root = "src/main.fol",
    });
    graph.install(app);
    return;
};
