pro[] build(): non = {
    var graph = .graph();
    var app = graph.add_exe({
        name = "std-cli",
        root = "src/main.fol",
        fol_model = "std",
    });
    graph.install(app);
    graph.add_run(app);
    return;
};
