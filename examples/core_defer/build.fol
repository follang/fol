pro[] build(): non = {
    var graph = .graph();
    var app = graph.add_exe({
        name = "core-defer",
        root = "src/main.fol",
        fol_model = "core",
    });
    graph.install(app);
    return;
};
